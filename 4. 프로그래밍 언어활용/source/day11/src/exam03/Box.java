package exam03;

public class Box<T> {
   // private T item;

    //▼ 앞에 <T>가 명시 돼있어야 지네릭메서드, 밑에 문장은 지네릭 클래스
    // (지네릭 클래스는) box가 객체가 될 때 t가 결정
  //  public void method(T str1, T str2) {

    }
//▼얘는 앞에 명시 돼있기 때문에 지네릭 메서드, 위에 T와 다른 T이다.
 //   public <T> void method(T str1, T str2) {

//    }
//}
